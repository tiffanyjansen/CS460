@model IEnumerable<WorldWideImporters.Models.Person>

@{
    ViewBag.Title = "Search";
}

<div class="center">
    <h2 style="font: large;">Client Search</h2>
    <div class="search-container">
        <form method="get">
            <input type="text" placeholder="Search.." name="search">
            <button type="submit">Submit</button>
        </form>
    </div>
</div>

<form method="post">
    <ul style="list-style-type: none;">
        @foreach (var item in Model)
        {
            <li><button type="submit" style="text-align: left; color: black;" name="ID" value=@item.PersonID>@Html.DisplayFor(modelItem => item.FullName)</button></li>
        }
    </ul>
</form>
